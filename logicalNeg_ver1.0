 * logicalNeg - implement the ! operator, using all of 
 *              the legal operators except !
 *   Examples: logicalNeg(3) = 0, logicalNeg(0) = 1
 *   Legal ops: ~ & ^ | + << >>
 *   Max ops: 12
 *   Rating: 4 
 */
int logicalNeg(int x) {
     /* only 0x0 ^ 0x01 will be 0x01, exploit this to compute, and exploit | to implement if
     *and try to transform non-zero numbers to one same number */
     
     
     return 2;
}
